#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "../package.h"



Vector* parse_version(char* v_str) {
	Vector* out = vector_new(sizeof(int), VECTOR_KEEP | VECTOR_ORDERED);
	
	char* buf;
	int temp;
	
	return out;
}
